+------+
| TODO |
+------+

Orcus:
	* F200 detection
	* LCD functions - blitter, yuv
	* Audio
	* SD card - write support
	* NAND - erase and write
	* F200 touchscreen
	* Set CPU clock
	* MMU and PU functions

readelf -s framebuffer.elf | grep "__params_addr" | grep -Po ': \K[0-9A-Fa-f]+ ' | xargs printf "0x%s"

remove timer alteration from sd.c, do timeout more like timerSleepNs to avoid interfering with user code making use of the timer

0x88000000 - 0x00 on an F100, 0xFF on an F200